####################################################
#   .gitattributes ~ mainly GH linguist settings   #
####################################################
# I want the GitHub repo to actually reflect the
# content of the repo, not all the files generated
about/** linguist-vendored
posts/** linguist-vendored
privacy/** linguist-vendored
####################################################
# Similarly, I have no idea how large index will
# eventually grow, so this is just so it doesn't
# become a purely HTML repository
index.html linguist-vendored
####################################################
# Again, these are generated by Emacs Org Export, so
# they're not really 'part of the repo' in a sense
config/** linguist-vendored
src/org-style.css linguist-vendored
####################################################

